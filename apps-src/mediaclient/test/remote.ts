// The D-pad harness moved to apps-src/_shared/test/ when a second app needed it.
// Re-exported here so this app's tests keep naming it where they always have.
export * from "../../_shared/test/remote";
